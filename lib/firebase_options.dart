// PLACEHOLDER — replace by running the FlutterFire CLI against your own
// Firebase project:
//
//   dart pub global activate flutterfire_cli
//   flutterfire configure
//
// That command overwrites this file with real per-platform options
// (API keys, app IDs, project ID). See README.md, "Firebase setup".

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart is a placeholder. Run `flutterfire configure` '
      'to generate real options for your Firebase project (see README.md).',
    );
  }
}
