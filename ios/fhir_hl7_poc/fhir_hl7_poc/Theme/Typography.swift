import SwiftUI

struct AppTypography {
    static let headerTitle = Font.custom("Arial", size: 24).bold()
    static let sectionTitle = Font.custom("Arial", size: 20).bold()
    static let labelText = Font.custom("Arial", size: 16).bold()
    static let bodyText = Font.custom("Arial", size: 16)
    static let secondaryText = Font.custom("Arial", size: 14)
} 